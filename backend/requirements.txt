# MermaOps — dependencias backend
# Python 3.12+
# Instalar: pip install -r backend/requirements.txt

# ── API Claude ────────────────────────────────────────────────────────────────
anthropic>=0.102.0          # Claude API (Opus 4.7, Sonnet 4.6, Haiku 4.5)

# ── Backend web ───────────────────────────────────────────────────────────────
fastapi>=0.136.0            # Framework API REST
uvicorn>=0.47.0             # Servidor ASGI
slowapi>=0.1.9              # Rate limiting (slowapi sobre Starlette)
httpx>=0.27.0               # Cliente HTTP async (requerido por FastAPI TestClient)

# ── Base de datos ─────────────────────────────────────────────────────────────
supabase>=2.30.0            # Cliente Supabase (PostgreSQL + Auth + Realtime)

# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot>=22.7   # Bot API oficial

# ── PDF y reportes ────────────────────────────────────────────────────────────
reportlab>=4.5.0            # Generación de PDF con branding MermaOps

# ── Scheduler ────────────────────────────────────────────────────────────────
APScheduler>=3.11.0         # Cron jobs: brief 07:30, semanal lun 06:00, etc.

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv>=1.2.0        # Carga de variables de entorno desde .env
requests>=2.34.0            # HTTP síncrono (advance_demo, Open-Meteo)

# ── Voz (opcional — Whisper) ──────────────────────────────────────────────────
openai>=2.37.0              # Cliente OpenAI para Whisper (si OPENAI_API_KEY configurado)

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=9.0.0               # Framework de tests
pytest-asyncio>=0.24.0      # Tests asíncronos

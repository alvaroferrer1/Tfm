import 'package:flutter_test/flutter_test.dart';

// Tests de widget requieren inicialización de Supabase — se usan integration tests.
// Este archivo sirve como placeholder para que flutter analyze no dé error.
void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
